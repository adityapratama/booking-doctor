<%= form_with(model: [:admin, hospital], local: true) do |form| %>
  <% if hospital.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(hospital.errors.count, "error") %> prohibited this hospital from being saved:</h2>

      <ul>
        <% hospital.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :city %>
    <%= form.select :city_id, City.all.sort{|a, b| a.name <=> b.name}.map{|c| [c.name, c.id]} %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
